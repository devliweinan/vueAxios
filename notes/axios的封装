axios封装
    封装的优势：
        可以统一处理错误，简化代码。
        每个请求的参数不一样，可以使用封装，统一代码的格式。
        全部接口在请求前同意处理相同的逻辑，比如，请求比较慢的时候，统一添加加载中的提示。使用请求拦截器
        统一url进行统一的管理。方便维护升级

步骤：创建一个文件夹。分模块管理请求，不同的业务模块封装到不同的文件里面。比如订单模块，商品模块。。。 